/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {appDir: true}
    
}

module.exports = {
    images: {
        domains: ['fakestoreapi.com' , "via.placeholder.com"]
    }
}, nextConfig
